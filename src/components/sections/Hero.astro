---
import { Image } from "astro:assets";
import heroBgExcavator from "../../assets/hero-bg-excavator.webp";
---

<section class="relative h-screen flex items-center justify-center overflow-hidden bg-slate-900">
  <div class="absolute inset-0 z-0">
    <Image 
      src={heroBgExcavator} 
      alt="Excavadora trabajando en una obra de Stralla Vial" 
      class="w-full h-full object-cover"
      loading="eager" 
      fetchpriority="high"
    />
    <div class="absolute inset-0 bg-slate-900 opacity-60"></div>
  </div>
  
  <div class="relative z-10 text-center px-4 max-w-5xl">
    <span class="font-body bg-yellow-500/50 w-fit mx-auto px-6 py-2 rounded-full text-white text-sm md:text-md font-bold tracking-widest uppercase mt-16 mb-4 block animate-fade-in-up delay-100">
      Más de 25 años de trayectoria
    </span>
    <h1 class="font-heading text-5xl md:text-7xl font-extrabold text-white mb-6 leading-tight animate-fade-in-up delay-200">
      Construyendo el <span class="text-yellow-500">futuro vial</span> del país.
    </h1>
    <p class="font-body text-xl text-slate-200 mb-8 max-w-2xl mx-auto animate-fade-in-up delay-300">
      Líderes en ingeniería, movimiento de suelos y alquiler de maquinaria pesada para proyectos de gran escala.
    </p>
    <div class="flex flex-col sm:flex-row gap-4 justify-center animate-fade-in-up delay-400">
      <a 
        href="#servicios" 
        class="bg-yellow-500 hover:bg-yellow-600 hover:text-white text-slate-900 px-8 py-4 rounded-lg font-bold transition-all transform hover:scale-105 active:scale-95 shadow-lg"
      >
        Nuestros Servicios
      </a>
      <a 
        href="#contacto" 
        class="border-2 border-white text-white hover:bg-white hover:text-slate-900 px-8 py-4 rounded-lg font-bold transition-all transform hover:scale-105 active:scale-95"
      >
        Contacto Directo
      </a>
    </div>
  </div>
</section>

<style>
  /* Animaciones básicas de entrada */
  @keyframes fade-in-up {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .animate-fade-in-up {
    animation: fade-in-up 0.8s ease-out forwards;
    opacity: 0; /* Asegura que el elemento esté oculto antes de la animación */
  }

  .delay-100 { animation-delay: 0.1s; }
  .delay-200 { animation-delay: 0.2s; }
  .delay-300 { animation-delay: 0.3s; }
  .delay-400 { animation-delay: 0.4s; }
</style>